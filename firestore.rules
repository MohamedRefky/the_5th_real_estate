rules_version = '2';

// ─── Firestore Security Rules — The 5th Real Estate ─────────────────
// Full read and write permissions for managing properties and buildings.

service cloud.firestore {
  match /databases/{database}/documents {

    match /{document=**} {
      allow read, write: if true;
    }
  }
}
