rules_version = '2';

// ─── Firebase Storage Security Rules — The 5th Real Estate ──────────
// Full read and write permissions for uploading property images.

service firebase.storage {
  match /b/{bucket}/o {

    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
